---
interface Props {
    className?: string;
}

const { className = "" } = Astro.props;
---

<div
    class={`grid grid-cols-1 md:grid-cols-3 gap-4 max-w-7xl mx-auto p-4 ${className}`}
>
    <slot />
</div>
